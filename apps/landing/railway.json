{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && bun run build"
  },
  "deploy": {
    "startCommand": "bunx serve dist -s -l 3000",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
