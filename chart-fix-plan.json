{
  "issue": "chart-not-updating-realtime",
  "date": "2026-01-12",
  "status": "fixed",
  "rootCause": "timestamp-unit-mismatch",
  "summary": "Binance WebSocket sends timestamps in milliseconds, but lightweight-charts expects seconds. TradeView passed milliseconds directly to ChartManager causing stale timestamps.",
  "fixes": [
    {
      "file": "src/components/trade_interface/TradeView.tsx",
      "line": 206,
      "change": "Convert data.startTime from milliseconds to seconds: Math.floor(data.startTime / 1000)"
    },
    {
      "file": "src/utils/chart_manager.ts",
      "method": "update()",
      "changes": [
        "Initialize lastUpdateTime in seconds: Math.floor(Date.now() / 1000)",
        "Use provided time directly (already in seconds) instead of dividing",
        "Document PriceUpdate.time expects seconds in JSDoc"
      ]
    }
  ],
  "verification": {
    "build": "cd testudo-web && bun run build",
    "manual": [
      "Open http://localhost:5173/trade/BTCUSDT",
      "Verify chart updates in real-time",
      "Test timeframe changes (1m, 5m, 1h)"
    ]
  }
}
